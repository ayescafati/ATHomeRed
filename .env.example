# Copy this to .env and fill in values. The app auto-loads this file.
# Prefer a single DATABASE_URL (works with Supabase and Postgres):
# Example Supabase direct connection string:
# DATABASE_URL=postgresql://postgres:<PASSWORD>@db.<project>.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=

# Alternatively, use individual variables (ignored if DATABASE_URL is set)
# DB_DIALECT=postgresql
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_NAME=athomedb
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_SSLMODE=require

# JWT secret (required by auth)
AT_HOME_RED_SECRET=dev-secret-key

# Token expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=60
